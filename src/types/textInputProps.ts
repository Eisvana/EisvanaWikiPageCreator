export interface TextInputProps {
  label: string;
  maxlength?: string | number;
  invalid?: boolean;
  errorMessage?: string;
  helpTitle?: string;
  helpImg?: string;
  tooltip?: string;
  initialValue?: string;
  placeholder?: string;
}
